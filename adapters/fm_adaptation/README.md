# Foundation-model adaptation adapter

Standard frozen, linear probe, LoRA/PEFT, partial fine-tuning, or full fine-tuning recipes. Every adapted checkpoint must retain source-weight and data provenance.
