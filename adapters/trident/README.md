# TRIDENT adapter

Use only after a task-specific preprocessing contract is frozen. Resolve the local TRIDENT installation from `local/local_resources.yaml` or verified auto-discovery. Never hardcode a user path into the public skill.
