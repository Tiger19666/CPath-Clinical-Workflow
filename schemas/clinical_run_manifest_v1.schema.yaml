type: object
required: [version, skill, references, stability_contract]
properties:
  version: {const: ClinicalRunManifest-v1}
  skill:
    type: object
    required: [name, version, git]
  references: {type: object}
  notes: {type: array}
  stability_contract: {type: object}
