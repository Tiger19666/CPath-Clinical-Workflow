type: object
required: [status, task_id, run_scope, skill, environment, inputs, outputs]
properties:
  status: {enum: [FROZEN]}
  task_id: {type: string}
  run_scope: {type: string}
  skill: {type: object}
  environment: {type: object}
  inputs: {type: object}
  outputs: {type: object}
