type: object
required: [status, study_scope, maximum_claim_level, allowed_claim_classes, forbidden_claim_classes, recommended_language, hard_rules]
properties:
  status: {enum: [PASS]}
  study_scope: {type: string}
  maximum_claim_level: {type: string}
  allowed_claim_classes: {type: array}
  forbidden_claim_classes: {type: array}
  recommended_language: {type: string}
  hard_rules: {type: array}
