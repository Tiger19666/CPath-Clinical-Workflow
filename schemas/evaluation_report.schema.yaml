type: object
required: [status, study_scope, split, n_patients, models, per_model, paired_comparisons, interpretation_guard]
properties:
  status: {enum: [DIAGNOSTIC_ONLY, SCIENTIFIC_EVALUATION_READY]}
  study_scope: {type: string}
  split: {type: string}
  n_patients: {type: integer}
  models: {type: array}
  per_model: {type: object}
  paired_comparisons: {type: object}
  interpretation_guard: {type: string}
