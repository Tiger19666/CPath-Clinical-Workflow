type: object
required: [task_id, references]
properties:
  task_id: {type: string}
  references:
    type: array
    items:
      type: object
      required: [reference_id, claimed, resolved, evidence_roles]
